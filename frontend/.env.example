# Base URL for the backend API. Leave unset in dev to use the Vite proxy (/api -> localhost:5000).
# VITE_API_URL=https://dfcci-threshold-3sbv.onrender.com

# Google OAuth client ID (also injectable via a hardcoded fallback in src/main.jsx)
# VITE_GOOGLE_CLIENT_ID=